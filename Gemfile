source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in lazy_value.gemspec.
gemspec

gem "sqlite3"

# Start debugger with binding.b [https://github.com/ruby/debug]
# gem "debug", ">= 1.0.0"

gem "sprockets-rails"
gem "pry"
gem "wrapped_print"

group :test do
  gem "capybara"
  gem "selenium-webdriver"
end

gem "turbo-rails"
